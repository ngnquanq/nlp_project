# Direct versions recorded by the completed E2 run manifest.
# PyTorch/CUDA remains installed from environments/llm.yml; this file pins the
# Python packages needed to reproduce that resolved software stack.
accelerate==1.14.0
bitsandbytes==0.50.1
datasets==4.8.5
peft==0.20.0
PyYAML==6.0.2
sacrebleu==2.6.0
# Evaluation-only dependency added after the historical training run.
sacremoses==0.2.0
transformers==4.57.6
trl==0.29.1
pytest==8.4.1
