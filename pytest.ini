[pytest]
pythonpath = code
testpaths = tests
markers =
    private_data: requires the restricted local corpus and is excluded from source-only CI
    private_model: requires the private E1 checkpoint and Fairseq data-bin
    private_e2_model: requires the private E2 adapter and a CUDA GPU
