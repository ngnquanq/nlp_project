# Predictions

Generated JSONL files belong here but are ignored until explicitly packaged for private submission. Every row must satisfy the schema enforced by `mt_pipeline.schema.PredictionRecord`.

